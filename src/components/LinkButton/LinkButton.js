import React, { useCallback } from "react";
import { Alert, Button, Linking } from "react-native";

export default function LinkButton({ href, title }) {
  return <Button onPress={null} title={title} />;
}
